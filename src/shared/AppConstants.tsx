/** The title/name of the application/project */
export const appTitle: string = "LunchBox";

/** The subtitle of the application/project */
export const appSubtitle: string = "poverty edition";

/** The string representation of the currrent version
 * (should not be used to compare versions - there should probably be a "build id" or something for that)
 */
export const appVersionString: string = "alpha";
