/** Name of the file containing preferences. */
export const PREFERENCES_FILENAME = 'preferences.json';

/** Name of the file containing configs. */
export const CONFIG_FILENAME = 'config.json';

/** Name of the file containing extension configs. */
export const EXT_CONFIG_FILENAME = 'extConfig.json';

/** Source to use when logging messages regarding services. */
export const SERVICES_SOURCE = 'Background Services';
